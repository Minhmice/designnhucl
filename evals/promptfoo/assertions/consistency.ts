import type { MetaEvaluationReport } from '../../../src/evals/index.js';
export function assertConsistency(report: MetaEvaluationReport, maxMeanStandardDeviation: number): { pass: boolean; score: number | null; reason: string } { const values = report.consistency?.criteria.flatMap(c => c.standardDeviation === null ? [] : [c.standardDeviation]) ?? []; const score = values.length ? values.reduce((s, x) => s + x, 0) / values.length : null; return { pass: score !== null && score <= maxMeanStandardDeviation, score, reason: score === null ? 'No assessed criteria.' : `Mean standard deviation: ${score}` }; }
