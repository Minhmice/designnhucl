export default function consistency(output) { const report = JSON.parse(output); const trust = report.trust; const pass = trust?.passed === true; const score = trust?.meanStandardDeviation === null || trust?.meanStandardDeviation === undefined ? 0 : Math.max(0, 1 - trust.meanStandardDeviation); return { pass, score, reason: trust ? `Độ tin cậy=${pass ? 'đạt' : 'chưa đạt'}; lặp=${trust.actualRepeats}/${trust.minimumRepeats}; tiêu chí đủ mẫu=${trust.completeCriterionSampleCount}; SD=${trust.meanStandardDeviation}; đồng thuận tuyệt đối=${trust.meanExactAgreement}; trong khoảng 1=${trust.meanWithinOneAgreement}; bao phủ judge=${trust.criterionCoverage}; đủ rubric=${trust.completeCriterionCoverage}; hỗ trợ bằng chứng=${trust.evidenceSupportRate}; numeric integrity=${trust.numericIntegrityRate}; hallucination proxy=${trust.hallucinationRate}; lý do=${trust.reasons.join(',')}` : 'Báo cáo độ tin cậy không có.' }; }
