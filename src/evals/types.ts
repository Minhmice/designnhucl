import type { EvidenceBundle, EvaluationContext, Finding, JudgeResult } from '../contracts.js';

export type EvaluationMode = 'frozen-judge-consistency' | 'pipeline-consistency' | 'calibration' | 'regression';
export type NumericSummary = { samples: number; mean: number | null; median: number | null; variance: number | null; standardDeviation: number | null; minimum: number | null; maximum: number | null; range: number | null; coefficientOfVariation: number | null };
export type CriterionConsistency = NumericSummary & { criterionId: string; exactAgreementRate: number | null; withinOneAgreementRate: number | null; distribution: Record<'0'|'1'|'2'|'3'|'4', number>; unobservedRate: number; notApplicableRate: number };
export type ClassificationStability = { samples: number; modalArchetype: string | null; archetypeAgreement: number | null; modalDesignLanguage: string | null; designLanguageAgreement: number | null; confidenceDistribution: Record<'high'|'medium'|'low', number>; archetypeEntropy: number | null; designLanguageEntropy: number | null };
export type FindingStability = { totalRuns: number; findings: Array<{ key: string; observedRuns: number; recurrenceRate: number; severityAgreement: number | null; evidenceAgreement: number | null; recommendationAgreement: number | null; stable: boolean }>; meanRecurrence: number | null; stableFindings: number; unstableFindings: number };
export type TrustEvaluation = { minimumRepeats: number; actualRepeats: number; sufficientRepeats: boolean; completeCriterionSampleCount: number; passed: boolean; reasons: string[]; thresholds: { maxMeanStandardDeviation: number; minExactAgreement: number; minWithinOneAgreement: number; minFindingRecurrence: number; minEvidenceExistence: number; minEvidenceSupport: number; minCriterionCoverage: number }; evidenceExistenceRate: number | null; evidenceSupportRate: number | null; unsupportedClaimRate: number | null; hallucinationRate: number | null; criterionCoverage: number | null; completeCriterionCoverage: number | null; schemaComplianceRate: number | null; numericIntegrityRate: number | null; meanStandardDeviation: number | null; meanExactAgreement: number | null; meanWithinOneAgreement: number | null; findingRecurrence: number | null };
export type EvalProvenance = { schemaVersion: 1; metricVersion: string; mode: EvaluationMode; sampleCount: number; runIds: string[]; evidenceHashes: string[]; model: string | null; provider: string | null; promptVersion: string | null; rubricVersion: string | null; createdAt: string };
export type PipelineSummary = { requestedRuns: number; successfulRuns: number; failedRuns: number; unscorableRuns: number; numericIntegrity: NumericIntegrityReport; runSummaries: Array<{ runId: string; executionStatus: string; assessmentStatus: string | null; accessReason?: string | null; scoreByDimension: Record<string, number | null>; findings: number; errors: string[] }> };
export type MetaEvaluationReport = { provenance: EvalProvenance; pipeline?: PipelineSummary; consistency?: { criteria: CriterionConsistency[]; dimensions: Record<string, NumericSummary> }; classificationStability?: ClassificationStability; findingStability?: FindingStability; trust?: TrustEvaluation; grounding?: unknown; adherence?: unknown; calibration?: unknown; regression?: unknown };
export type FrozenJudgeInput = { bundle: EvidenceBundle; context: EvaluationContext; repeats: number; judge: (input: { bundle: EvidenceBundle; context: EvaluationContext; repeat: number }) => Promise<JudgeResult[]>; runIds?: string[] };
export type PipelineRepeatInput = { repeats: number; run: (repeat: number) => Promise<import('../contracts.js').EvaluationRun>; };
export type NumericIntegrityReport = { valid: boolean; checkedMetrics: number; invalidMetrics: Array<{ runId: string; auditId: string; metric: string; reason: string }> };export type SemanticFindingMatcher = (a: Finding, b: Finding) => boolean | Promise<boolean>;
